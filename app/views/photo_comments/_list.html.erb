<% comments.each do |comment| %>
  <div>
    <strong><%= comment.user.short_name %></strong>
    <%= comment.content %>
    <small><%= time_tag comment.created_at, "#{time_ago_in_words comment.created_at} ago" %></small>
  </div>
<% end %>